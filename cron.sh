#!/bin/bash

# Cron is an alternative to at, while using it is advisable to use full paths to commands

# crontab -e or crontab -u user -e, then format it according to as given

# if all is correct then successfully scheduled